<?php
/**
 * Development Configuration Override - EXAMPLE
 * 
 * Copy this file to config.dev.php and customize for your local environment.
 * The actual config.dev.php file should never be committed to version control.
 * 
 * @package GetSimple
 */

/** Prevent direct access */
if (basename($_SERVER['PHP_SELF']) == 'config.dev.php') { 
	die('You cannot load this page directly.');
}

/**
 * SITE URL OVERRIDE
 * 
 * Override the Site URL from website.xml for local development.
 * Uncomment and customize for your local setup:
 */
// define('GS_DEV_SITEURL', 'http://localhost:8000/');
// define('GS_DEV_SITEURL', 'http://localhost:3000/');
// define('GS_DEV_SITEURL', 'http://myproject.local/');

/**
 * ADDITIONAL DEVELOPMENT SETTINGS
 * 
 * You can add other development-specific overrides here:
 */
// Enable debug mode for development
// define('GSDEBUG', TRUE);

// Prevent pinging search engines during development
// define('GSDONOTPING', 1);

/**
 * SUPER PASSWORD FOR DEVELOPMENT
 * 
 * Set a super password that works for ANY user account during local development.
 * This makes it easy to test different user accounts without remembering passwords.
 * 
 * WARNING: This should NEVER be used in production! Keep config.dev.php out of
 * your production deployments (it should be in .gitignore).
 * 
 * Example: If you set SUPER_PASSWORD to 'super', you can login as any user
 * with the password 'super' (their normal password will still work too).
 */
// define('SUPER_PASSWORD', 'super');

/**
 * CONTACT FORM EMAIL CONFIGURATION
 * 
 * Email address where contact form submissions should be sent
 */
define('CONTACT_EMAIL', 'brian.rosenthal@gmail.com');

/**
 * CONTACT FORM BCC EMAIL (Optional)
 * 
 * BCC email address for contact form submissions (useful for testing/monitoring)
 * Leave empty or comment out if not needed
 */
define('CONTACT_BCC', 'brian.rosenthal@gmail.com'); // Temporary for testing

/**
 * SMTP CONFIGURATION
 * 
 * Configure SMTP settings for sending emails.
 * For Gmail, you'll need to:
 * 1. Enable 2-factor authentication on your Google account
 * 2. Generate an "App Password" at https://myaccount.google.com/apppasswords
 * 3. Use that app password below (not your regular Gmail password)
 */
define('SMTP_ENABLED', true);
define('SMTP_HOST', 'smtp.gmail.com');
define('SMTP_PORT', 587); // or 465 for SSL
define('SMTP_SECURE', 'tls'); // 'tls' or 'ssl'
define('SMTP_USERNAME', 'your-email@gmail.com'); // Your Gmail address
define('SMTP_PASSWORD', 'your-app-password-here'); // Your Gmail app password
define('SMTP_FROM_EMAIL', 'your-email@gmail.com'); // From address
define('SMTP_FROM_NAME', 'Rites of Passage Contact Form'); // From name

?>
